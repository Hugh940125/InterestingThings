<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hugh.interesting">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- 固定屏幕的方向，处理键盘滑出事件，防止activity关闭或者重启 -->
        </activity>
        <activity android:name=".activity.SVGActivity" />
        <activity android:name=".house_plan.HousePlanActivity">
            <intent-filter> <!-- URL Scheme启动 -->
                <!-- 必有项 -->
                <action android:name="android.intent.action.VIEW" />
                <!-- 表示该页面可以被隐式调用，必须加上该项 -->
                <category android:name="android.intent.category.DEFAULT" />
                <!-- 协议部分 -->
                <data
                    android:host="house_plan_activity"
                    android:scheme="url_scheme" />
            </intent-filter>
            <intent-filter>
                <action android:name="emms.intent.action.check_authorization" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="emms.intent.category.authorization" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RobotActivity" />
        <activity android:name=".keyboard.KeyBoardActivity" />
        <activity android:name=".red_point.RedPointActivity" />
        <activity android:name=".activity.ActivityForTestRobotView" />
        <activity android:name=".anim.AnimActivity" />
        <activity android:name=".sliding_conflict.SlidingConflictActivity" />
        <activity android:name=".dl.DLActivity" />
        <activity android:name=".view_binder.ViewBinderTestActivity" />
        <activity android:name=".CoordinatorLayout.CoordinatorActivity"></activity>
    </application>

</manifest>